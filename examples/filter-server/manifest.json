{
  "name": "unix-filter-example",
  "version": "1.0.0",
  "instructions": "A minimal MCP server whose implementation is a Unix filter.",
  "tools": [
    {
      "name": "hello",
      "description": "Return a greeting.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  ]
}
